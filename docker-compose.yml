services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: cafeuser
      POSTGRES_PASSWORD: cafepassword
      POSTGRES_DB: cafefinder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cafeuser -d cafefinder"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata: